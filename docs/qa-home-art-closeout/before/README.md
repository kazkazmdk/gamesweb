# Before

Homepage screenshots from `cursor/home-visual-rebuild-c08e` (`d124d11`) live in `docs/qa-home-rebuild/`.

Gameplay JPGs used on that head are copied here as `art/*-hero.jpg` and `art/*-backdrop.jpg`.
